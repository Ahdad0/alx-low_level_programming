#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @int: an integer
 * Return: -num if less 0, num if greater than 0
 */
int _abs(int)
{
	int num;

	if (num < 0)
	{
		return (-num);
	}
	else if (num > 0)
	{
		return (num);
	}
}
