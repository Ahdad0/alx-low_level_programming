#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: an integer
 * Return: -num if less 0, num if greater than 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
